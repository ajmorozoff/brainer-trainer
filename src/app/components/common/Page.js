import React, { useEffect, useState } from 'react';

const styles = {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'flex-start'
}

const Page = (props) => {
    return (
        <div style={styles}>
            Here is a page
        </div>
    )
}